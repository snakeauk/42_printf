#include "ft_printf.h"

char    ft_itoa_base(int num)
{
    int   base;

    base = 16;
    if (num < 0)
        num *= -1;
    while ()
        
}

void    ft_printf_x(t_format f_fmt)
{
    int num;

    va_list ap;
    num = va_arg(ap, int);
    
    ft_itoa(num);

}
